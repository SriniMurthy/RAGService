spring.application.name=markets
spring.profiles.active=data-ingestion
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.embedding.options.retry-template=#{retryTemplate}
spring.ai.openai.chat.options.model=gpt-4o

# Explicitly define the datasource properties
spring.datasource.url=jdbc:postgresql://localhost:5432/RAGJava
spring.datasource.username=smurthy
spring.datasource.password=ide123Hosahaadu
spring.datasource.driver-class-name=org.postgresql.Driver

#spring.ai.vectorstore.pgvector.initialize-schema=true
spring.ai.vectorstore.pgvector.schema-name=public
spring.ai.vectorstore.pgvector.initialize-schema=false


# Suppress WARN logs from FileSystemFontProvider
logging.level.org.apache.pdfbox.pdmodel.font.FileSystemFontProvider=ERROR

spring.docker.compose.lifecycle-management=start_only

# ========== DynamoDB Chat Memory Configuration ==========
# NOTE: DynamoDB configuration has been moved to application.yaml for better organization
# The configuration in application.yaml will be used
#
# If you need to override these settings, uncomment the lines below:
# chat.memory.dynamodb.enabled=true
# chat.memory.dynamodb.endpoint=http://localhost:8000
# chat.memory.dynamodb.region=us-west-2
# chat.memory.dynamodb.table-name=chat_history
# chat.memory.dynamodb.ttl-days=7
